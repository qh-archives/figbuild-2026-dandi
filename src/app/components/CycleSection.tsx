import { CycleSectionContent } from "../../imports/IPhone1653";

/** Cycle section with heading and circle chart (same as Heart Rate expanded view). */
export function CycleSection() {
  return <CycleSectionContent />;
}
